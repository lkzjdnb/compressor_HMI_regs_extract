# compressor HMI registers extract
Simple python notebook to extract registers definition from HyET EHC compressor HMI.

## Usage
Start jupyter :

`jupyter lab`

Open get_registers_defs.ipynb and run it.
